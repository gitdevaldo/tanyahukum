# Scripts Directory Index

This folder is organized by function:

- `scripts/crawler/` → crawling and rate-test tools
- `scripts/ingest/` → ingestion pipeline to Qdrant
- `scripts/migrate_mongo_to_qdrant.py` → one-off migration utility

Use each subfolder README for detailed usage:

- `scripts/crawler/README.md`
- `scripts/ingest/README.md`
